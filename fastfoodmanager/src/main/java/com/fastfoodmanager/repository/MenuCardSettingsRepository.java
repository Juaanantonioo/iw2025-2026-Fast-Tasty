package com.fastfoodmanager.repository;

import com.fastfoodmanager.domain.MenuCardSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MenuCardSettingsRepository extends JpaRepository<MenuCardSettings, Long> {
}
