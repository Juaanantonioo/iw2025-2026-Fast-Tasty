spring:
  datasource:
    url: jdbc:h2:mem:ffm;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    defer-datasource-initialization: true  # <- importante para que data.sql vaya después de JPA
  sql:
    init:
      mode: always  # extra por si acaso

  mail:
    host: smtp.gmail.com
    port: 587
    username: fastandtasty.restaurante@gmail.com
    password: ftdlgvifnqgbgrkm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp
        debug: true  # Solo para desarrollo

h2:
  console:
    enabled: true


logging:
  level:
    com.vaadin.flow.server: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: INFO
